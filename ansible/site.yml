---
- name: Configure Ollama LXC 202
  hosts: ollama_hosts
  become: true

  roles:
    - nvidia_userspace   # install matching userspace libs (no kernel module — driver lives on host)
    - ollama
    - open_webui
    - anything_llm
