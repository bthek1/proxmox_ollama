[project]
name = "proxmox-ollama-tools"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "ollama>=0.4",
    "rich>=13",
]

[dependency-groups]
dev = [
    "pytest>=8",
]
